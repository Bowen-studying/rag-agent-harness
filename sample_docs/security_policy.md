# Agent 安全边界

Agent 只能调用登记在 allowlist 中的工具。读取代码和查询日志默认允许；删除文件、修改权限、访问生产密钥和执行数据库写入需要人工确认。

所有工具调用必须记录工具名、参数摘要、结果状态、延迟和失败原因。日志中不得写入 API Key、访问令牌或用户隐私原文。

检测到越权工具名时，Harness 应立即拒绝请求并标记 failure_type=tool_boundary。

